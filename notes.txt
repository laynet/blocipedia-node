make comparePassword function using bcrypt compare logic with hashed password

PASSPORT - create a POST route for the login endpoint - use local passport strategy via the passport middleware to authenticaet user

SECURE ROUTES - add requires to routes/users.js, create wuth middleware and add middleware to all requests that need auth
need to authenticate before calling controller

signout route


**Maybe no "confirm password" on sign in form

**validation.js maybe need passwordConfirmation for matching instead of password???